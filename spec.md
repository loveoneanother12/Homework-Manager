# 과제 채점 평가서 시스템 — 프로젝트 명세서

> 이 문서는 Claude Code가 읽고 프로젝트를 구현하기 위한 명세서입니다.
> 구현 시 이 문서의 설계 원칙과 제약을 반드시 준수하십시오.

---

## 0. 한 줄 요약

학원 채점조교가 **숫자만 입력**하면, 단원별 가중치에 따라 KPI를 자동 계산하고, **알고리즘 기반(비-AI)** 으로 강사용 코멘트를 조합 생성하여, 학생별 평가서 카드를 만들고, 반 단위로 **A4 한 페이지당 10명**씩 PDF로 출력하는 웹앱.

---

## 1. 배경 및 목적

### 1.1 문제 상황
- 현재 정량 체크 항목이 '숙제 이행률' 하나뿐이며, 정성 평가는 강사 코멘트와 카톡 소통에만 의존.
- 강사는 물리적 시간이 부족하고, 채점조교(알바)는 질적 평가를 맡기기 어려운 역량 제약이 있음.
- 강사가 수업 중 관찰하는 정성평가와 조교가 과제에서 관찰하는 정량평가 사이에 **데이터 사일로**가 존재.

### 1.2 핵심 목표
1. 교재 내 채점/피드백의 꼼꼼함으로 '관리 잘 되는 모습'을 학부모에게 보여준다.
2. 채점 데이터를 디지털화하여 정량/정성 평가를 강사에게 전달한다.
3. 채점조교의 질적 수준에 결과가 좌우되지 않도록 **자동화·시스템화**한다.

### 1.3 설계 우선순위
- **조교는 숫자 입력만**: 어떤 문제를 틀렸는지 문항별로 기록하지 않는다. "안 푼 n개", "풀고 틀린 m개" 식의 집계 숫자만 입력.
- **결과의 일관성**: 동일 입력 → 동일 코멘트. 예측 가능성 우선.
- **강사가 통제 가능**: 코멘트 문장 풀과 단원 가중치를 강사가 직접 관리.

---

## 2. 채점 단계 및 KPI 정의

채점은 **1차 채점**(과제 첫 제출)과 **2차 채점**(오답노트 후 재채점)으로 나뉜다.

### 2.1 1차 채점 입력 항목 (조교 입력)
모두 정수 숫자 입력 박스.

| 입력 필드 | 의미 |
|-----------|------|
| `total_count` | 전체 문항 수 |
| `not_attempted` | 안 푼 문제 수 (시도 자체 없음) |
| `gave_up` | 손 못 댄 문제 수 (시도했으나 진행 불가) |
| `wrong_attempted` | 풀고 틀린 문제 수 (풀이 있으나 오답) |
| `process_score` | 풀이과정 서술 수준: `good` / `needs_work` / `poor` / null (4택, null이면 서술 평가 미출력) |

> **정답 문항 수**는 입력하지 않고 계산: `correct = total_count - not_attempted - gave_up - wrong_attempted`

### 2.2 2차 채점 입력 항목 (조교 입력, 오답노트 제출 후)
1차에서 틀린 문항(= `not_attempted + gave_up + wrong_attempted`)에 한해 입력.

| 입력 필드 | 의미 |
|-----------|------|
| `retry_total` | 오답노트로 다시 작성한 문항 수 |
| `retry_correct` | 다시 풀어서 맞은 문항 수 |
| `retry_wrong` | 다시 풀어도 틀린 문항 수 |
| `retry_gave_up` | 여전히 손 못 댄 문항 수 |
| `note_quality` | 오답노트 서술 성의: `good` / `needs_work` / `poor` / null (4택, null이면 서술 평가 미출력) |

### 2.3 자동 계산 KPI (파생 지표)

**1차**
- 이행률 = `(total_count - not_attempted) / total_count`
- 정답률 = `correct / (total_count - not_attempted)` ← 분모에서 안 푼 문제 제외
- 오답 유형 비율 = 각각 `not_attempted / total_count`, `gave_up / total_count`, `wrong_attempted / total_count`

**2차**
- 오답노트 이행률 = `retry_total / (1차 오답 수)`
- 오답 이해 도달률 = `retry_correct / (1차 오답 수)`
- 반복 오류율 = `retry_wrong / (1차 오답 수)`
- 완전 미해결율 = `retry_gave_up / (1차 오답 수)`

### 2.4 문항별 vs 과제별 데이터 구분
- **과제별(질적) 데이터**: 이행률, 정답률, 서술 수준 등 회차 전체에 대한 인상 → 코멘트 생성의 주재료.
- **문항별(정오) 데이터**: 본 시스템에서는 문항 단위로 저장하지 않고, **집계 숫자**로만 환원하여 처리. (조교 입력 부담 최소화 목적)

---

## 3. 단원별 가중치 시스템

### 3.1 개념
같은 KPI라도 단원 성격에 따라 코멘트에서 강조할 지점이 다르다. 단원이 어떤 능력을 시험하는지(계산 정확도 / 논증·서술 / 그래프·도형 해석 / 활용·응용)에 따라 평가 가중치를 달리한다.

### 3.2 기본 프리셋 (초기 제공) — 5종
강사가 단원 생성 시 아래 중 하나를 고르고, 이후 세부 조정 가능. 가중치 레벨은 **낮음 / 중간 / 높음** 3단계로만 운용한다.

| 프리셋 | 이행률 | 정답률 | 서술 | 코멘트 방향 힌트 |
|--------|:---:|:---:|:---:|------------------|
| 계산정확형 | 중간 | **높음** | 낮음 | 계산 실수·검산 습관 강조 |
| 논증서술형 | 중간 | 중간 | **높음** | 논리 전개·서술 완성도 강조 |
| 그래프해석형 | 중간 | **높음** | 중간 | 그래프 오독→답 오류, 그래프 정확성 강조 |
| 활용 및 응용형 | 중간 | **높음** | **높음** | 문제 해석·풀이 설계 + 최종 계산 둘 다 |
| 기본형 | 중간 | 중간 | 중간 | 균형 평가, '손 못 댐' 비율 모니터링 (default) |

### 3.2.1 단원별 프리셋 매핑 (초기 시드 데이터)
`hw_unit_presets` 초기 데이터로 아래 매핑을 사용한다. 강사가 이후 자유롭게 변경 가능.

| 과목 | 단원 (세부목차) | 매핑 프리셋 | 매핑 근거 |
|------|----------------|------------|-----------|
| 공통수학1 | 다항식 (연산 / 나머지정리·인수분해) | 계산정확형 | 풀이 방향 명확, 계산 실수에서 점수 갈림 |
| 공통수학1 | 방정식과 부등식 — 복소수 | 기본형 | 개념 도입부, 손 못 댐 모니터링 |
| 공통수학1 | 방정식과 부등식 — 이차방정식 | 계산정확형 | 판별식·근과 계수, 계산 중심 |
| 공통수학1 | 방정식과 부등식 — 이차방정식과 이차함수 | 그래프해석형 | 그래프·최대최소, 그래프 오독 시 연쇄 오류 |
| 공통수학1 | 방정식과 부등식 — 여러 가지 방정식 / 연립일차부등식 | 계산정확형 | 절차적 계산 정확도가 핵심 |
| 공통수학1 | 방정식과 부등식 — 이차부등식 | 그래프해석형 | 이차함수 그래프 해석으로 해결 |
| 공통수학1 | 경우의 수 (순열 / 조합) | 활용 및 응용형 | 상황 해석·경우 분류 설계가 관건 |
| 공통수학1 | 행렬 (행렬의 연산) | 계산정확형 | 연산 규칙 적용·계산 실수 중심 |
| 공통수학2 | 도형의 방정식 (평면좌표 / 직선 / 원 / 도형의 이동) | 그래프해석형 | 좌표·도형 시각화 해석이 핵심 |
| 공통수학2 | 집합과 명제 — 집합 | 기본형 | 개념 도입부, 균형 평가 |
| 공통수학2 | 집합과 명제 — 명제 | 논증서술형 | 증명·필요충분·대우, 논리 전개 평가 |
| 공통수학2 | 함수와 그래프 (함수 / 유리·무리함수) | 그래프해석형 | 그래프 개형·정의역 해석 중심 |
| 대수 | 지수함수와 로그함수 — 지수와 로그 | 계산정확형 | 지수·로그 성질 계산 중심 |
| 대수 | 지수함수와 로그함수 — 지수·로그함수(그래프) | 그래프해석형 | 그래프 개형·점근선 해석 |
| 대수 | 삼각함수 — 삼각함수(호도법·그래프) | 그래프해석형 | 주기·그래프 해석 중심 |
| 대수 | 삼각함수 — 삼각함수의 활용(사인·코사인 법칙) | 활용 및 응용형 | 도형 상황 해석 + 최종 계산 결합 |
| 대수 | 수열 — 등차·등비수열 / 수열의 합 | 계산정확형 | 일반항·합 공식 적용 계산 중심 |
| 대수 | 수열 — 수학적 귀납법 | 논증서술형 | 증명 절차·서술 완성도 평가 |
| 미적분Ⅰ | 함수의 극한과 연속 (극한 / 연속) | 기본형 | 개념 진입 난도, 손 못 댐 모니터링 |
| 미적분Ⅰ | 미분 — 미분계수와 도함수 | 계산정확형 | 미분 계산 정확도 중심 |
| 미적분Ⅰ | 미분 — 도함수의 활용 | 활용 및 응용형 | 접선·증감·극값·실생활 응용 설계 + 계산 |
| 미적분Ⅰ | 적분 — 부정적분과 정적분 | 계산정확형 | 적분 계산 정확도 중심 |
| 미적분Ⅰ | 적분 — 정적분의 활용 | 활용 및 응용형 | 넓이 등 상황 해석 + 계산 결합 |

### 3.3 UI 단순화 원칙
- 강사는 숫자 가중치를 직접 입력하지 않는다.
- "이 단원에서 가장 중요한 건?" 형태의 **선택지 방식**으로 받아 내부적으로 가중치로 환산.
- 가중치는 코멘트 문장 블록의 **선택 우선순위**에만 영향을 준다(파트 2 참조). 점수 자체를 왜곡하지 않는다.

---

## 4. 코멘트 생성 — 알고리즘 조합 방식 (AI API 미사용)

### 4.1 원칙
- 외부 AI API를 호출하지 않는다. **비용 0원, 결과 일관성 확보.**
- 입력 KPI 조합을 조건 분기로 판별하여, 사전 작성된 문장 블록을 파트별로 조합한다.

### 4.2 코멘트 구조 (6파트)

코멘트는 **1차 코멘트**와 **2차 코멘트**로 분리 저장된다.

- **1차 코멘트** (`generated_comment`) = 파트1 + 파트2 + (파트3)
- **2차 코멘트** (`generated_comment_2`) = 파트4 + (파트5) + (파트6)

괄호 표시 파트는 조건부 출력(조건 미충족 시 해당 파트 문장 전체 생략).

| 파트 | 이름 | 채점단계 | 출력방식 | 판정 기준 |
|------|------|----------|----------|----------|
| 1 | 이행 총평 | 1차 | 항상 | 이행률 |
| 2 | 오답 유형 진단 | 1차 | 항상 | gave_up / not_attempted / wrong 비율 + 단원 프리셋 |
| 3 | 풀이 서술 평가 | 1차 | 조건부 | process_score (null이면 미출력) |
| 4 | 오답 교정 결과 | 2차 | 항상 | 이해 도달률 |
| 5 | 반복 오류·미해결 경고 | 2차 | 조건부 | retry_wrong / retry_gave_up (둘 다 0이면 미출력) |
| 6 | 오답노트 서술 | 2차 | 조건부 | note_quality (null이면 미출력) |

---

#### 파트 1 — 이행 총평 (항상 출력)

| 조건 | 문장 |
|------|------|
| 이행률 ≥ 90% | 부여된 과제를 빠짐없이 완수하였습니다. 꾸준한 학습 태도가 잘 자리잡고 있습니다. |
| 70% ≤ 이행률 < 90% | 과제 대부분을 충실히 시도하였습니다. 일부 미완성된 문항은 마무리 학습으로 클리닉 지도하겠습니다. |
| 이행률 < 70% | 과제를 잘 시도하였으나, 이행률에서 다소 보완이 필요합니다. 학습 분량을 끝까지 완성할 수 있도록 추가 지도하겠습니다. |
| fallback | 과제 제출을 잘 확인하였습니다. |

#### 파트 2 — 오답 유형 진단 (항상 출력)

gave_up_rate, not_attempted_rate, wrong_rate 중 최댓값이 15% 미만이면 `no_issue`. 이상이면 최댓값 항목으로 분기. wrong이 최대일 때 단원 프리셋이 계산정확형·그래프해석형이면 `wrong_dominant_calc`, 나머지는 `wrong_dominant_general`.

| 조건 | 문장 |
|------|------|
| 손 못 댄(gave_up) 비율 높음 | 개념을 처음 적용하는 문항에서 어려움을 느낀 부분이 있어, 해당 개념의 기본 문제를 다시 짚어보면 도움될 것으로 생각됩니다. |
| 안 푼(not_attempted) 비율 높음 | 남아있는 미시도 문항에 대해서는 안 풀리는 문제더라도 끝까지 시도하는 연습이 더해지면 좋겠습니다. |
| 풀고 틀린 비율 높음 (일반) | 전 문항에 적극적으로 풀이를 시도하였습니다. 마무리 단계의 정확도를 높이는 부분에서 보완이 필요합니다. |
| 풀고 틀린 비율 높음 (계산정확형·그래프해석형) | 풀이 접근은 좋으나, 계산 마무리와 검산 습관을 더하면 정확도가 한층 올라갈 것으로 생각됩니다. |
| 우수 / 특이사항 없음 | 오답 유형은 특이 경향 없이 안정적으로 문제를 잘 해결하였습니다. |
| fallback | 문항별 채점을 잘 확인하였습니다. |

#### 파트 3 — 풀이 서술 평가 (조건부: process_score ≠ null)

단원 프리셋이 논증서술형·활용 및 응용형이면 `_logic` 변형, 나머지는 `_general` 변형 선택.

| 조건 | 문장 |
|------|------|
| 우수 (논증서술형·활용 및 응용형) | 문제풀이와 함께 풀이과정의 논리 전개가 짜임새 있게 서술되었습니다. |
| 우수 (그 외) | 문제풀이와 함께 풀이과정을 깔끔하게 잘 서술하였습니다. |
| 보완 필요 (논증서술형·활용 및 응용형) | 문제풀이와 함께 풀이과정에서 논리 전개를 한 단계씩 나누어 꼼꼼히 서술하는 연습이 있으면 좋겠습니다. |
| 보완 필요 (그 외) | 문제풀이와 함께 풀이과정의 줄맞춤, 표기법 준수, 단락화를 신경써서 꼼꼼히 서술하는 연습이 있으면 좋겠습니다. |
| 미흡 | 문제풀이와 함께 사고 흐름이 드러나도록 풀이과정을 차근차근 적어가는 연습도 더해지면 좋겠습니다. |
| null (특이사항 없음) | *(미출력)* |

#### 파트 4 — 오답 교정 결과 (항상 출력, 2차 데이터 존재 시)

| 조건 | 문장 |
|------|------|
| 이해 도달률 ≥ 70% | 오답노트 재채점 결과 지난 과제가 잘 교정되었습니다. 자기주도 복습이 잘 이루어지고 있습니다. |
| 40% ≤ 도달률 < 70% | 오답노트 재채점 결과 상당수 문항이 교정되었습니다. 남은 문항은 개별 클리닉으로 재검하겠습니다. |
| 도달률 < 40% | 오답노트 재채점 결과 다시 공부할 문항이 확인되었습니다. 남은 문항은 개별 클리닉으로 재검하겠습니다. |
| fallback | 오답노트 재채점을 잘 확인하였습니다. |

#### 파트 5 — 반복 오류·미해결 경고 (조건부)

| 조건 | 문장 |
|------|------|
| retry_wrong > 0, retry_gave_up = 0 | 오답노트에서도 반복적으로 틀린 문항은 개별 클리닉 지도하겠습니다. |
| retry_gave_up > 0, retry_wrong = 0 | 오답노트에서도 반복적으로 미해결된 문항은 개별 클리닉 지도하겠습니다. |
| retry_wrong > 0, retry_gave_up > 0 | 오답노트에서 추가적인 지도가 필요한 문항은 개별 클리닉 지도하겠습니다. |
| 둘 다 0 | *(미출력)* |

#### 파트 6 — 오답노트 서술 (조건부: note_quality ≠ null)

| 조건 | 문장 |
|------|------|
| 우수 | 풀이과정도 꼼꼼히 잘 정리하였습니다. |
| 보완 필요 | 풀이과정을 잘 작성하였고, 줄맞춤이나 표기 방식을 추가로 꼼꼼히 신경쓰면 더 좋겠습니다. |
| 미흡 | 풀이과정 서술을 더 꼼꼼히 신경쓰면 좋겠습니다. |
| null (특이사항 없음) | *(미출력)* |

---

### 4.3 Fallback 필수
- 항상 출력되는 파트(1, 2, 4)에서 조건에 매칭되는 문장이 없으면, 해당 파트의 **fallback 문장**이 출력되어야 한다. 빈 코멘트는 금지.

### 4.4 클리닉 플래그
- 2차에도 틀린 문항(`retry_wrong > 0`)이 있거나 완전 미해결율이 30% 이상이면 카드에 **클리닉 대상 표시** 노출.
- 2차 데이터 없는 경우, gave_up_rate ≥ 30%이면 클리닉 플래그.

### 4.5 강사용 문장 풀 관리 화면
- 강사가 파트 1~6 문장을 추가·수정·삭제할 수 있는 관리 화면 제공.
- 편집 시 **변경 전 미리보기**와 **되돌리기** 제공.
- 기본 문장 풀은 코드에 하드코딩으로도 백업(DB 초기화/오삭제 대비).
- `hw_sentence_blocks` 테이블의 `part` 컬럼: `'1'` ~ `'6'` (문자열).

### 4.6 2차 채점의 크로스 레코드 표시
- 2차 채점은 **이번 회차와 다른 날짜**에 수행된다(예: 이번 회차 1차 채점 → 다음 회차에 2차 채점 수행).
- `hw_homework_records.second_session_date` = 2차 채점을 실제로 수행한 날짜.
- 이번 회차 평가서 카드에는 **이번 회차 1차 코멘트(상단) + 이번 회차에 2차 채점된 전 회차 코멘트(하단)**를 함께 표시.
- 조회 방법: `second_session_date = 현재 조회 날짜`인 레코드를 조회 → 해당 전 회차의 2차 데이터를 현재 카드에 표시.

---

## 5. 평가서 카드 & PDF 출력

### 5.1 카드 사양 (PDF 출력 기준)
- 카드 1장 크기: **가로 10cm × 세로 5cm**.
- 카드에 담길 내용:
  - 학생 이름, 학년, 단원명, 채점 날짜
  - 이행률 · 정답률 (수치 + 막대 그래프)
  - 오답 유형: **미완결** (손 못 댄) / **오답** (풀고 틀린) — 비율 표시. 안 푼 항목은 이행률에 반영되므로 카드에서 별도 표기 생략.
  - 풀이과정 서술 수준 (우수/보통/미흡 뱃지, process_score가 null이면 표시 생략)
  - 2차 채점 데이터 (있을 때만): 이해 도달률, 반복 오류율
  - 1차 알고리즘 생성 코멘트
  - 2차 오답노트 피드백 코멘트 (크로스 레코드, 있을 때만)
  - 클리닉 대상 플래그 (해당 시)
- 카드에는 코멘트 **수동 편집 필드**를 둔다(누락 케이스 보완용).

### 5.2 PDF 출력
- 출력 결과물은 **PDF 파일**로 생성(브라우저 인쇄 대화상자가 아닌 파일 다운로드).
- **A4 1페이지당 10명** 배치 (5행 × 2열, 10cm×5cm 카드 10장이 A4에 맞음).
- 반 단위로 'PDF 다운로드' 버튼 → 해당 반 전원 카드를 PDF로 일괄 생성.
- **구현 방식**:
  - 웹 미리보기에서는 `editable=true` 카드(textarea) 렌더링 — 코멘트 직접 수정 가능.
  - PDF 캡처 전용으로 화면 밖(`left: -9999px`)에 `editable=false` 카드(div, 자동 높이)를 숨겨서 렌더링.
  - `html2canvas`로 `editable=false` 카드 캡처 → `jsPDF`로 A4 10분할 좌표에 배치 합성.
  - (브라우저 기본 `@media print`는 환경별 편차가 커서 사용하지 않음.)
- 카드 레이아웃은 **고정 픽셀값**으로 확정하여 환경 간 편차를 제거.

---

## 6. 데이터 모델

> **모듈화 대비**: 향후 대형 학원관리 앱(Supabase 기반, 학생-반 다대다 이미 구축됨)에 모듈로 삽입할 예정.
> 따라서 아래 원칙을 처음부터 지킨다.

### 6.1 모듈화 핵심 원칙
1. **`student_id`는 외부 주입**: 학생 데이터를 본 앱에서 자체 관리하지 않는다. 컴포넌트는 `studentId`를 props 또는 URL 파라미터로 받는다. (현 단계에서는 테스트용 students 테이블을 별도로 두되, 통합 시 제거/연결 가능하게 분리)
2. **테이블 네이밍 접두사 `hw_`**: 통합 시 대형 앱 테이블과 충돌 방지.
3. **React 진입점**: `<HomeworkGrading studentId={id} />` 형태로 어디든 꽂을 수 있게 구성.
4. 현 단계는 **별도 Supabase 프로젝트**로 시작. 테이블만 이식 가능하게 설계.

### 6.2 테이블

```
hw_classes               (반 정보)
- id (uuid, pk)
- class_name             // 반 이름 (unique)
- created_at

hw_students              (학생 정보 — 통합 시 외부 students로 대체)
- id (uuid, pk)
- name
- grade                  // 학년: '초1'~'초6', '중1'~'중3', '고1'~'고3', 'N수', nullable
- created_at

hw_class_students        (반-학생 N:M 조인)
- class_id (uuid, fk → hw_classes, ON DELETE CASCADE)
- student_id (uuid, fk → hw_students, ON DELETE CASCADE)
- PRIMARY KEY (class_id, student_id)
// 한 학생이 여러 반에 소속 가능, 반별로 채점 기록 분리

hw_unit_presets          (단원 가중치 설정)
- id (uuid, pk)
- unit_name
- subject                // '공통수학1' | '공통수학2' | '대수' | '미적분I' 등
- preset_type            // 'calculation_accuracy' | 'proof_description' | 'graph_interpretation' | 'application' | 'default'
- weight_completion      // 이행률 가중치: 'low' | 'mid' | 'high'
- weight_accuracy        // 정답률 가중치: 'low' | 'mid' | 'high'
- weight_process         // 서술 가중치: 'low' | 'mid' | 'high'
- sort_order
- created_at

hw_homework_records      (채점 입력 + KPI + 코멘트)
- id (uuid, pk)
- student_id (uuid, fk → hw_students)
- class_id (uuid, fk → hw_classes)   // 다중 반 수강 시 반별 기록 분리
- unit_id (uuid, fk → hw_unit_presets)
- round                  // 1 | 2
- session_date           // 1차 채점 수업 날짜 (date)
- second_session_date    // 2차 채점 수업 날짜 (date, nullable)
- total_count
- not_attempted
- gave_up
- wrong_attempted
- process_score          // 'good' | 'needs_work' | 'poor' | null
- retry_total            // 2차에만 사용 (nullable)
- retry_correct
- retry_wrong
- retry_gave_up
- note_quality           // 'good' | 'needs_work' | 'poor' | null
- generated_comment      // 1차 알고리즘 생성 코멘트
- manual_comment         // 1차 수동 편집 코멘트 (nullable)
- generated_comment_2    // 2차 알고리즘 생성 코멘트 (nullable)
- manual_comment_2       // 2차 수동 편집 코멘트 (nullable)
- clinic_flag            // boolean
- created_at

hw_sentence_blocks       (코멘트 문장 풀)
- id (uuid, pk)
- part                   // '1' | '2' | '3' | '4' | '5' | '6'
- condition_key          // 조건 식별자 (아래 참조)
- sentence_text
- is_active
- updated_at
```

**`hw_sentence_blocks.condition_key` 값 목록**

| part | condition_key 값 |
|------|-----------------|
| '1' | `completion_high`, `completion_mid`, `completion_low`, `fallback` |
| '2' | `gave_up_dominant`, `not_attempted_dominant`, `wrong_dominant_general`, `wrong_dominant_calc`, `no_issue`, `fallback` |
| '3' | `good_logic`, `good_general`, `needs_work_logic`, `needs_work_general`, `poor` |
| '4' | `understanding_high`, `understanding_mid`, `understanding_low`, `fallback` |
| '5' | `repeat_error_only`, `unresolved_only`, `both` |
| '6' | `note_good`, `note_needs_work`, `note_poor` |

### 6.3 통합 시나리오
통합 시점에 `hw_homework_records.student_id`를 대형 앱의 `students.id`에 외래키로 연결하고, `hw_students`와 `hw_classes`, `hw_class_students`는 대형 앱의 해당 테이블로 대체. 나머지 테이블(`hw_unit_presets`, `hw_homework_records`, `hw_sentence_blocks`)은 그대로 이식.

---

## 7. 입력 유효성 검사

### 7.1 클라이언트 (실시간)
- `not_attempted + gave_up + wrong_attempted ≤ total_count`
- 모든 수치는 0 이상의 정수
- 2차: `retry_correct + retry_wrong + retry_gave_up ≤ 1차 오답 수`
- 2차: `retry_total ≤ 1차 오답 수`
- 위배 시 저장 버튼 비활성화 + 안내 메시지.

### 7.2 저장 직전
- 계산된 KPI **미리보기**를 보여주어 조교가 육안 확인 후 저장.

### 7.3 서버 (Supabase)
- 동일 유효성 검사를 한 번 더 수행(클라이언트 우회 방지). 가능하면 DB 제약/RLS로 보강.

---

## 8. 기술 스택

- **Frontend**: Vite + React + Tailwind CSS v3
- **Routing**: React Router v6 (HashRouter — GitHub Pages SPA 대응)
- **Backend / DB**: Supabase (별도 프로젝트로 시작)
- **PDF**: html2canvas + jsPDF
- **배포**: GitHub Pages
- **AI API**: 사용하지 않음

---

## 9. 화면 구성 (페이지 & 라우트)

앱은 4개 탭으로 구성된다.

### 탭 1 — 채점 (`/`)
반 목록 화면. 수업 날짜 필터, 반 검색, 요일 필터 제공.
- 반 클릭 → 학생 목록 (`/class/:className`)
  - 날짜별 채점 완료 여부 표시
  - 학생 클릭 → 채점 입력 (`/student/:studentId/grade?date=YYYY-MM-DD&class=반이름`)
    - Step 1 (1차 채점): 단원 선택 + 숫자 입력 + process_score 선택 + KPI 미리보기 + 저장
    - Step 2 (2차 채점): 이전 회차 대기 레코드 목록 → 선택 → 숫자 입력 + note_quality 선택 + 저장
  - 반 평가서 미리보기/PDF 버튼 → 반 평가서 (`/class/:className/preview?date=YYYY-MM-DD`)
    - 채점 완료 학생 카드 목록 (editable, 코멘트 직접 수정 가능)
    - PDF 다운로드 버튼 (화면 밖 editable=false 카드로 캡처)

### 탭 2 — 학생/반 관리 (`/manage`)
- **반 관리 섹션**: 반 추가 / 편집 / 삭제. 반 행 클릭 시 아코디언 확장 → 소속 학생 칩 + 제거 버튼 + 학생 검색·추가 UI.
- **학생 관리 섹션**: 학생 추가(이름 + 학년) / 삭제. 학생 이름 클릭 → 학생 이력 새 탭 오픈.
- N:M 구조: 한 학생이 여러 반에 소속 가능. 반별로 채점 기록 분리.

### 학생 이력 (`/student/:studentId/history`)
- 해당 학생의 전체 평가서 카드를 반별·날짜 역순으로 열람.
- `editable=false` 카드 (수정 불가).
- 새 탭에서 열림.

### 탭 3 — 단원 관리 (`/units`)
- 과목별 아코디언 구조 (공통수학1/2, 대수, 미적분I).
- 단원 추가·편집·삭제.
- 프리셋 5종 선택 + 세부 가중치(낮음/중간/높음) 3축 조정.

### 탭 4 — 문장 풀 관리 (`/sentences`)
- 파트 1~6 섹션별 문장 목록.
- 문장 편집(미리보기 포함) / 활성·비활성 / 삭제 / 추가.
- 기본값으로 초기화 버튼 (DB 전체 재설정).

---

## 10. 리스크 및 헷징 플랜

중요도: 🔴 높음 / 🟡 중간 / 🟢 낮음

| ID | 리스크 | 중요도 | 헷징 |
|----|--------|--------|------|
| R1 | PDF 출력 품질 환경별 편차 | 🔴 | html2canvas+jsPDF로 이미지 캡처 후 합성, editable=false 카드 별도 캡처, 고정 픽셀 레이아웃, 초기 실제 출력 테스트 |
| R2 | 문장 조합 경우의 수 누락 | 🔴 | 파트별 독립 분기 + fallback 기본 문장, 파일럿 테스트(실데이터 10~20건), 카드 내 수동 편집 필드 |
| R3 | 조교 입력 오류(논리 불가 값) | 🔴 | 실시간 유효성 검사 + 저장 전 KPI 미리보기 + 서버단 재검증 |
| R4 | 단원 가중치 설계의 주관성 | 🟡 | 선택지 방식 UI, 기본 프리셋 5종, 변경 이력 기록 |
| R5 | 학생·반 데이터 관리 복잡도 | 🟡 | N:M 구조, class_id로 반별 기록 분리, hw_ 접두사, 통합 대비 스키마 설계 |
| R6 | 모바일 사용 환경 | 🟡 | 입력 폼 모바일 반응형, 출력은 PC 전용 허용 |
| R7 | 문장 풀 관리 중 실수 | 🟢 | 변경 전 미리보기, 코드 하드코딩 백업, 기본값 초기화 버튼 |
| R8 | Supabase 무료 플랜 휴면 | 🟢 | 주 1회 헬스체크 자동화, 규모 확대 시 Pro 전환 검토 |

---

## 11. 구현 완료 범위 및 향후 과제

### 11.1 구현 완료
- 단원 선택 + 1차/2차 숫자 입력 + 유효성 검사
- KPI 자동 계산 (정답률 분모: 시도 문항 수)
- 6파트 알고리즘 코멘트 생성 (1차 3파트 + 2차 3파트, fallback 포함)
- 크로스 레코드 2차 채점 표시 (second_session_date 기반)
- 평가서 카드 렌더링 + 코멘트 수동 편집
- A4 10분할 PDF 출력 (editable=false 전용 캡처 카드)
- 학생/반 N:M 구조 (hw_class_students)
- 반별 채점 기록 분리 (class_id)
- 학생 학년 정보 (grade)
- 학생 이력 페이지
- 채점/학생반관리/단원관리/문장풀관리 4탭 구성

### 11.2 향후 과제
- 누적 데이터 기반 단원별 장단점 분석 대시보드
- 1차 대비 2차 개선폭 추이 그래프
- 학생별 이행률·정답률 장기 추이
- 대형 학원관리 앱으로의 모듈 통합

---

## 12. 구현 시 준수 사항 요약

1. `student_id`는 절대 하드코딩하지 말 것 — 외부 주입 구조.
2. 모든 테이블/주요 식별자에 `hw_` 접두사.
3. AI API 호출 코드 작성 금지 — 코멘트는 순수 알고리즘 조합.
4. PDF는 파일 다운로드 방식, html2canvas+jsPDF, editable=false 카드로 캡처.
5. 모든 KPI 입력에 3중 유효성 검사(실시간/저장전/서버).
6. 항상 출력 파트(1, 2, 4)는 fallback 보장 — 빈 코멘트 금지.
7. 입력 폼 모바일 반응형.
8. URL 파라미터 변경 시 기존 파라미터 보존 (`setSearchParams(prev => ...)` 패턴).
9. 반별 기록 분리를 위해 채점 저장 시 `class_id` 반드시 포함.
